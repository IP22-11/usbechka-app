<Window x:Class="usbechka_app.pages.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:usbechka_app.pages"
        Title="Окно администратора" 
        Height="450" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <local:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
    </Window.Resources>

    <Border Margin="10" Background="#FF2D2D2D" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="Black" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid Height="30" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Администратор" 
                          Foreground="White"
                          VerticalAlignment="Center"
                          Margin="10,0,0,0"
                          FontSize="14"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="MinimizeButton" 
                            Content="_" 
                            Style="{StaticResource WindowButtonStyle}"
                            Click="MinimizeWindow"/>
                    <Button x:Name="CloseButton" 
                            Content="✕" 
                            Style="{StaticResource WindowButtonStyle}"
                            Click="CloseWindow"/>
                </StackPanel>
            </Grid>

            <Grid Margin="10,50,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid x:Name="tables" 
                          Grid.Column="0"
                          Margin="0,0,5,0"
                          Style="{StaticResource AdminDataGrid}"
                          ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Номер стола" Binding="{Binding number_table}" Width="*"/>
                        <DataGridTextColumn Header="Статус" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="{Binding is_reserved, Converter={StaticResource BoolToStatusConverter}}"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <DataGrid x:Name="menu" 
                          Grid.Column="1"
                          Margin="5,0,0,0"
                          Style="{StaticResource AdminDataGrid}"
                          ColumnHeaderStyle="{StaticResource ColumnHeaderStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Название" Binding="{Binding title}" Width="*"/>
                        <DataGridTextColumn Header="Описание" Binding="{Binding description}" Width="*"/>
                        <DataGridTextColumn Header="Цена" Binding="{Binding price, StringFormat={}{0} руб.}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="1" Grid.Column="0"
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            Margin="0,10,0,0">
                    <Button Content="Добавить стол" 
                            Click="ButtonAddTable" 
                            Style="{StaticResource AdminButton}"/>

                    <Button Content="Удалить стол" 
                            Click="ButtonDeleteTable" 
                            Style="{StaticResource AdminButton}"
                            Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1"
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            Margin="0,10,0,0">
                    <Button Content="Добавить блюдо" 
                            Click="ButtonAddDelishes" 
                            Style="{StaticResource AdminButton}"/>

                    <Button Content="Удалить блюдо" 
                            Click="ButtonDeleteDelishes" 
                            Style="{StaticResource AdminButton}"
                            Margin="10,0,0,0"/>
                </StackPanel>

                <Button Content="Назад" 
                        Grid.Row="1" Grid.ColumnSpan="2"
                        Click="ButtonBack" 
                        Style="{StaticResource BackButton}"
                        HorizontalAlignment="Center"
                        Margin="0,10,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>